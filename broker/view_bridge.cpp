// broker/view_bridge.cpp — ViewBridge façade (header-only delegation).

#include "broker/view_bridge.hpp"

namespace re::broker {
// Delegation is inline in header; translation unit keeps STATIC non-empty.
} // namespace re::broker
